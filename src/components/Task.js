import React from 'react'

const Task = (props) => {

    const{
        title, done, category
    } = props;
    return (
        <div>
            <h1>Task</h1>
        </div>
    )
}

export default Task
